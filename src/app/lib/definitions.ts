export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type Car = {
  id: string;
  description: string;
  make: string;
  model: string;
  cost: number;
};
